#include<stdio.h>
#include<math.h>
int main(){

    int n,m;
    scanf("%d %d",&n,&m);
    if(m==1&&n==1)
    {
        printf("%d",1);
    }
    else
    {
        printf("%d",(a>n/2?a-1:a+1));
    }

    return 0;
}