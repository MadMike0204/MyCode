#include<stdio.h>
#include<math.h>
int main(){

    float h,b,c,d,i,x,y;
    scanf("%f",&h);
    int n;
    scanf("%d",&n);
    while(n--){
        
        b = pow(0.5,n);
        c = b * h;

    }

    return 0;
}